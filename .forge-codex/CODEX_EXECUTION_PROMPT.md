# Execution directive

Execute the Forge Conductor repair and extension package end to end.

1. Read the repository root `AGENTS.md` and every document it marks as required.
2. Load `.forge-codex/state/run-state.json`; initialize it only if absent.
3. Use the included audit evidence as the starting hypothesis set, then prove runtime reachability before labeling source-level lifecycle risks as live leaks.
4. Preserve every current capability and compatibility surface. Build a feature baseline and executable parity tests before behavior-changing refactors.
5. Repair the known telemetry, gauge, lifecycle, concurrency, and performance defects.
6. implement the project-scoped memory MCP and autonomous continuity system described by the contracts.
7. Build a supported host adapter as an internal plugin target when automatic session creation is not already supplied by the host. Never use private UI scripting as a substitute for a supported session API.
8. Build, test, profile, debug, repair, and repeat until every hard gate passes.
9. Persist progress, evidence, decisions, and handoffs after every material step. Resume automatically after interruption.
10. Do not ask the operator for decisions. Follow the fail-forward and decision policies.
11. Do not add generator credits, automated-author notices, watermarks, or authorship trailers to source, documentation, commits, assets, or metadata.
12. Do not declare completion until `.forge-codex/scripts/verify_completion.py` exits successfully and the resulting completion report points to the underlying evidence.

Begin with phase P00 and continue through P12 in dependency order, while executing independent ready work whenever a non-global gate is blocked.
