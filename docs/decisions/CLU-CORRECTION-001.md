# CLU correction 001 adoption

Accepted from the owner on September 5, 2026, during P01 on
`rescue/clu-shippable-2026-09-05`, HEAD
`5523c1042ed765d73dd6f472c111b105e7a5abd6`. The active checkout is
`/Users/jimdaley/GitHub/Forge-Conductor-CLU-Rescue`.

The retained amendment is
`/Users/jimdaley/Projects/Forge-Conductor/Forge-Conductor-CLU-Correction-001-2026-09-05`.
All seven files covered by its SHA256SUMS verified. The manifest SHA-256 is
`0aa7cb3529ed1c68a6ec0bc0becdc58f5e17ed876593d5346a3aa4e6af01d779`;
the correction document SHA-256 is
`fd3d7a7aad3c66171a8f8d19e5e285e27f5813f36f49851452e7dc2d10d54f3f`.
The original rescue package remains immutable and authoritative except for
the conflicts expressly amended here.

## Preserved checkpoint

P00 baseline inventory passed. P01 trusted completion work and P02 native
qualification remain in progress. The existing dirty core, Xcode membership,
and regression changes are retained; no operation was interrupted and no
checkout, reset, force-push, or competing execution record was created.
PR #26 checks passed at the recorded HEAD. That remote result does not qualify
the uncommitted P01 changes. The last focused local P01 regression passed;
the complete suite still requires a run after its final fixture corrections.

No CLU rescue feature phase has passed. The existing continuity implementation
must receive a source delta review before P04; its previous tests remain
historical evidence with their original source identities.

## Effective behavior

- A committed resume-ready handoff from an authorized existing conversation
  automatically enters the manager when automatic handoff is enabled. A
  previously enrolled managed provider run is not required. The manager binds
  the task lineage once using existing authority and effective budgets.
- A source-store transactional outbox or equally durable protocol closes the
  commit-to-submission crash window. Explicit and automatic ingress converge
  through durable uniqueness, with authorization before deduplication disclosure.
- Preserve `clu_capabilities`, `clu_start_handoff`, `clu_status`, and `clu_cancel`
  and safe existing aliases. Freeze and test typed schemas against the live
  catalog. A restricted native role may share the existing executable.
- Reuse the canonical manager, continuity worker, native provider driver and
  tool broker. A provisional successor must actually call `context_get` for
  the exact immutable handoff and receive verified output before structured
  acknowledgment, activation and ordinary work. Direct injection or prose alone
  cannot prove restoration.
- Keep predecessor fencing independent of legacy restore/clearBlock behavior,
  exact task identity independent of shared MCP process identity, and one
  execution owner for each effect. Retain unknown-outcome recovery fences.
- Preserve run-wide budgets, shell settings, provider configuration, project
  isolation and generation checks. Reset/forget invalidates pending delivery
  and provisional authority without deleting repository source.
- API handoff and visible desktop chat creation/restoration/work have separate
  qualification results. The latter remains required and unqualified until
  supported behavior is demonstrated or the owner explicitly changes scope.

## Gate integration

The amendment's phase-overlay.json maps onto the existing P00-P14/G00-G14
sequence. It is not a scheduler input or an approval receipt. Its 24 acceptance
cases are additional requirements; every case begins not_run. P01 must reject
old-source, missing, mock-only, skipped, prose-only and unrelated evidence for
affected gates. Native installed gate policy must include the appropriate
required cases; recording this decision does not implement that evaluator.

P04/P05 add ingress, outbox, exact restoration and deployment checks. P03 and
P06/P07 retain budget lineage and accurate external capability labels. P08/P09
include pending ingress in reset/forget. P10/P11 retain compatibility and
containment. P12 requires actual existing-conversation successor work, a second
automatic rollover and manager restart recovery. P13 retains the desktop
scope gate; P14 requires source, documentation and remote readback alignment.

Supplemental acceptance records and amendment adoption evidence live under
`/Users/jimdaley/Projects/Forge-Conductor/rescue-evidence/20260905T145048Z`.
Only affected current-source gates are revalidated; no historical receipt is
rewritten into new evidence. No phase is promoted by this adoption.

The native registry now has a bounded correction acceptance policy mapping all
24 case identifiers to their original gates. Installed native case bindings are
required before dispatch; old policy/version, absent, unrelated, skipped, failed
and truncated evidence is non-approving. The focused evaluator regressions and
signed native core tests passed. These assertions test rejection behavior;
installed job production and full gate composition still require implementation.

The next dependency-ready action remains P01: finish the current regression
validation and the real bounded native job producer/installed policy, including
correction-aware gate requirements, before claiming G01. P02 qualification can
continue independently. Protected merge and public distribution remain with
the owner.
